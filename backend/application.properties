# Vue Reading App Application Configuration

# Server Configuration
server.port=8080
#server.servlet.context-path=/api
cors.allowed-origins=http://localhost:4040
cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
cors.allowed-headers=*
cors.allow-credentials=true
#?????

# Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/vue_reading_app?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=13603994106
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA/Hibernate Configuration
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.properties.hibernate.format_sql=true

# JdbcTemplate Configuration
spring.jdbc.template.query-timeout=30

# Logging Configuration
logging.level.root=INFO
logging.level.com.vue.readingapp=DEBUG
logging.level.org.springframework.web=DEBUG
logging.level.org.hibernate=ERROR

# JSON Configuration
spring.jackson.date-format=yyyy-MM-dd HH:mm:ss
spring.jackson.time-zone=GMT+8

# File Upload Configuration
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Actuator Configuration (if needed)
management.endpoints.web.exposure.include=health,info

# JWT Configuration
jwt.secret=vueReadingAppSecretKey
jwt.expiration=86400000

# Redis Configuration (optional)
spring.redis.host=localhost
spring.redis.port=6379
spring.redis.password=
spring.redis.timeout=2000ms
spring.redis.lettuce.pool.max-active=8
spring.redis.lettuce.pool.max-idle=8
spring.redis.lettuce.pool.min-idle=0
# ????
# QQ?? SMTP ??
spring.mail.host=smtp.qq.com
spring.mail.port=587
spring.mail.username=2898311413@qq.com
spring.mail.password=iiodyvggahradegi
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
# ????
app.frontend.url=http://localhost:8080